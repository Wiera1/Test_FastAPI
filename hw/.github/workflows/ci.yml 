name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: [ "main" ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install "black>=24.0.0"
          pip install isort flake8
          pip install mypy flask flask-sqlalchemy pytest

      - name: Run unit tests (pytest)
        run: |
          pytest

      - name: Run mypy
        run: |
          mypy hw

      - name: Run black (check only)
        run: |
          black --diff --diff .

      - name: Run isort (check only)
        run: |
          isort --check-only hw

      - name: Run flake8
        run: |
          flake8 hw

      - name: Run unit tests (pytest)
        run: pytest
